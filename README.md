# AquaMarket

Working aquarium marketplace MVP.

## Render settings
Build command: `pip install -r requirements.txt`
Start command: `gunicorn app:app`

## Local test
`pip install -r requirements.txt`
`flask --app app seed`
`flask --app app run`
